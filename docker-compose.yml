version: '3.9'

services:

  airport-service:
    build: ./airport-service
    ports:
      - "8000:8000"

  itinerary-service:
    build: ./itinerary-service
    ports:
      - "8001:8001"
    depends_on:
      - airport-service

  frontend:
    build: ./frontend
    ports:
      - "3000:80"
